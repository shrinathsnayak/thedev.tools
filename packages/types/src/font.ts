/**
 * Font-related type definitions
 */

export interface FontPair {
  heading: {
    name: string;
    family: string;
    category: string;
  };
  body: {
    name: string;
    family: string;
    category: string;
  };
  description: string;
  css: string;
}

